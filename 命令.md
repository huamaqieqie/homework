# 进入 Python 环境
source /data1/Johnny/challenge/wrf/.venv/bin/activate

# 进入仓库，并设置缓存/输出目录到 $SERVER_REPO/outputs
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO
source env_cache.sh

# 确认不会写到 /root
echo "LEWM_OUTPUT_ROOT=$LEWM_OUTPUT_ROOT"
echo "HOME=$HOME"
echo "XDG_CACHE_HOME=$XDG_CACHE_HOME"
echo "STABLE_PRETRAINING_CACHE_DIR=$STABLE_PRETRAINING_CACHE_DIR"
echo "STABLEWM_HOME=$STABLEWM_HOME"
echo "TMPDIR=$TMPDIR"

# 训练
MODE=train \
DATA_CONFIG=pusht \
DATASET_NAME=/manifoldai-training/johnny/challenge/lewm-pusht/datasets/pusht_expert_train.h5 \
OUTPUT_MODEL_NAME=lewm_15 \
SUBDIR=my_run \
MAX_EPOCHS=15 \
BATCH_SIZE=128 \
bash tools/jepa_eval/run_train_eval_template.sh

# 训练启动后，在终端日志中找到这一行：
# [RegistryLogger] run_id=... run_dir=... — sidecar.json + summary.json + metrics.csv will live here
# 新的 run_dir 应该在：
# /data1/Johnny/challenge/wrf/homework/outputs/.cache/stable-pretraining/runs/...

# 评估
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO
source env_cache.sh

# 确认不会写到 /root
echo "LEWM_OUTPUT_ROOT=$LEWM_OUTPUT_ROOT"
echo "HOME=$HOME"
echo "XDG_CACHE_HOME=$XDG_CACHE_HOME"
echo "STABLE_PRETRAINING_CACHE_DIR=$STABLE_PRETRAINING_CACHE_DIR"
echo "STABLEWM_HOME=$STABLEWM_HOME"
echo "TMPDIR=$TMPDIR"

MODE=eval \
EVAL_CONFIG=pusht.yaml \
POLICY=<你的checkpoint路径或相对STABLEWM_HOME的policy路径> \
NUM_EVAL=50 \
EVAL_BUDGET=300 \
bash tools/jepa_eval/run_train_eval_template.sh

# 监控训练曲线
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO
source env_cache.sh

# 确认不会写到 /root
echo "LEWM_OUTPUT_ROOT=$LEWM_OUTPUT_ROOT"
echo "HOME=$HOME"
echo "XDG_CACHE_HOME=$XDG_CACHE_HOME"
echo "MPLCONFIGDIR=$MPLCONFIGDIR"
echo "TMPDIR=$TMPDIR"

# 把这里替换成训练日志里显示的 run_dir
export RUN_DIR=<训练日志里的run_dir>
MODE=watch \
METRICS_LOG=$RUN_DIR/metrics.csv \
PLOT_OUT=$SERVER_REPO/outputs/jepa_eval/training \
WATCH_INTERVAL=60 \
bash tools/jepa_eval/run_train_eval_template.sh

# 导出 JEPA latents
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO
source env_cache.sh

# 确认不会写到 /root
echo "LEWM_OUTPUT_ROOT=$LEWM_OUTPUT_ROOT"
echo "HOME=$HOME"
echo "XDG_CACHE_HOME=$XDG_CACHE_HOME"
echo "STABLE_PRETRAINING_CACHE_DIR=$STABLE_PRETRAINING_CACHE_DIR"
echo "STABLEWM_HOME=$STABLEWM_HOME"
echo "TMPDIR=$TMPDIR"

# 把这里替换成真实 checkpoint。
# 可以是 Lightning .ckpt，也可以是 save_pretrained 生成的 .pt/.pth，或包含这些文件的目录。
export CKPT_PATH=<你的checkpoint路径>

MODE=export_latents \
DATA_CONFIG=pusht \
DATASET_NAME=/manifoldai-training/johnny/challenge/lewm-pusht/datasets/pusht_expert_train.h5 \
LATENT_CHECKPOINT=$CKPT_PATH \
LATENT_SPLIT=val \
LATENT_MAX_SAMPLES=1024 \
LATENT_OUT=$SERVER_REPO/outputs/jepa_eval/latents \
BATCH_SIZE=128 \
bash tools/jepa_eval/run_train_eval_template.sh
