# 进入环境
source /data1/Johnny/challenge/wrf/.venv/bin/activate
# 训练
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO
MODE=train \
DATA_CONFIG=pusht \
DATASET_NAME=/manifoldai-training/johnny/challenge/lewm-pusht/datasets/pusht_expert_train.h5 \
OUTPUT_MODEL_NAME=lewm_15 \
SUBDIR=my_run \
MAX_EPOCHS=15 \
BATCH_SIZE=128 \
bash tools/jepa_eval/run_train_eval_template.sh

# 评估
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO

MODE=eval \
EVAL_CONFIG=pusht.yaml \
POLICY=<checkpoint-or-policy-path-relative-to-STABLEWM_HOME> \
NUM_EVAL=50 \
EVAL_BUDGET=300 \
bash tools/jepa_eval/run_train_eval_template.sh

# 监控训练曲线
export SERVER_REPO=/data1/Johnny/challenge/wrf/homework
cd $SERVER_REPO

MODE=watch \
METRICS_LOG=/data1/Johnny/challenge/wrf/homework/outputs/jepa_eval/logs/train_pusht_my_run.log \
PLOT_OUT=$SERVER_REPO/outputs/jepa_eval/training \
WATCH_INTERVAL=60 \
bash tools/jepa_eval/run_train_eval_template.sh